name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  erlang:
    name: Erlang Tests + Dialyzer
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Beam
        uses: erlef/setup-beam@v1
        with:
          otp-version: '28'
          rebar3-version: '3.24'

      - name: Install build deps
        run: sudo apt-get update && sudo apt-get install -y cmake libelf-dev

      - name: Cache rebar3
        uses: actions/cache@v4
        with:
          path: |
            _build
            ~/.cache/rebar3
          key: erlang-${{ hashFiles('rebar.lock', 'rebar.config') }}
          restore-keys: |
            erlang-

      - name: Cache uBPF
        uses: actions/cache@v4
        with:
          path: third-party/
          key: ubpf-${{ runner.os }}-${{ hashFiles('scripts/build_ubpf.sh') }}

      - name: Build uBPF
        run: ./scripts/build_ubpf.sh

      - name: CI gate
        run: make check

  dsl:
    name: Elixir DSL Tests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Beam
        uses: erlef/setup-beam@v1
        with:
          otp-version: '28'
          elixir-version: '1.18'
          rebar3-version: '3.24'

      - name: Compile Erlang (dependency)
        run: rebar3 compile

      - name: Compile DSL
        run: cd dsl && mix deps.get && mix compile

      - name: Test DSL
        run: cd dsl && mix test
